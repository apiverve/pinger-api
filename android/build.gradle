plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.12'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'pinger-api'
            version = '1.1.12'

            from components.java

            pom {
                name = 'Domain and IP Pinger'
                description = 'Pinger is a simple tool for checking if a URL is up. It returns a boolean value indicating whether the URL is up or not.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/pinger-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/pinger-api.git'
                    url = 'https://github.com/apiverve/pinger-api'
                }
            }
        }
    }
}
